android_test {
    name: "UsageStatsTest",
    // Only compile source java files in this apk.
    srcs: ["src/**/*.java"],
    static_libs: ["android-support-v4"],
    certificate: "platform",
    platform_apis: true,
}
