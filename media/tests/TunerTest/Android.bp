android_test {
    name: "mediatunertest",

    srcs: ["**/*.java"],

    libs: [
        "android.test.runner",
        "android.test.base",
    ],

    static_libs: [
        "androidx.test.rules",
        "testng"
    ],

    platform_apis: true,
    certificate: "platform",
}
