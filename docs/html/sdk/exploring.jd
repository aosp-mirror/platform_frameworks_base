page.title=Exploring the SDK
walkthru=1

@jd:body




<p>If you're eager to start developing, we suggest you start with <a
href="{@docRoot}training/basics/firstapp/index.html">Building Your First App</a> or follow other
<a href="#Next">next steps</a> below.</p>


<table>
<tr>
<th colspan="3">Name</th><th>Description</th>
</tr>
<tr>
<td colspan="3"><code>add-ons/</code></td>
<td>Contains add-ons such as external libraries that are available on some
devices and emulator skins for specific devices. </td>
</tr>
<tr>
<td colspan="3"><code>docs/</code></td>
<td>A full set of Android developer documentation in HTML format.</td>
</tr>
<tr>
<td colspan="3"><code>platform-tools/</code></td>
<td>Contains platform-dependent development tools that may be updated with each platform release.
The platform tools include the Android Debug Bridge ({@code adb}) as well as other tools that you
don't typically use directly.</td>
</tr>
<tr>
<td colspan="3"><code>platforms/</code></td>
<td>Contains subdirectories for each Android platform version you've downloaded.  </td>
</tr>
<tr>
<td style="width:2em;"></td>
<td colspan="2"><code>android-<em>&lt;api-level&gt;</em>/</code></td>
<td>Contains the SDK platform for a specific version, for example "android-11". Each platform
directory includes the
Android library (<code>android.jar</code>) that is used to compile applications against the
platform version among other files such as the emulator skins supported by the platform.</td>
</tr>
<tr>
<td colspan="3"><code>samples/</code></td>
<td>Contains subdirectories for each set of sample code you've downloaded for a given Android
version.</td>
</tr>
<tr>
<td colspan="3"><code>sources/</code></td>
<td>Contains subdirectories for each set platform source code you've downloaded for a given Android
version.</td>
</tr>
<tr>
<td colspan="3"><code>tools/</code></td>
<td>Contains the SDK Tools that are platform-independent, such
as the emulator, the Android SDK Manager, the AVD Manager, <code>ddms</code>,
<code>hierarchyviewer</code>
and more.</td>
</tr>
<tr>
<td colspan="3"><code>SDK Manager.exe</code></td>
<td>On Windows only. A shortcut that launches the Android SDK
Manager tool, which you use to add packages to your SDK.</td>
</tr>

</table>


<p class="note"><strong>Tip:</strong> For easy access to the SDK tools from a command line, add the
location of the SDK's <code>tools/</code> and
<code>platform-tools</code> to your <code>PATH</code> environment variable.</p>
